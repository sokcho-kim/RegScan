# 작업일지 - 2026-04-16

## 작업 내역

### 1. [RegScan] 브리핑 파이프라인 V2 설계 확정

#### 배경

4/15 배치 파이프라인 실행 결과, unified briefing에서 "브리핑 본문은 '없음'이지만..." 생성.
근본 원인: LLM이 팩트 생성 + 기사 작성을 동시에 하는 구조적 결함.
가드레일/프롬프팅으로 해결 불가 — 파이프라인 재설계 필요.

#### 확정된 4-Step 구조

```
Step 1: 코드 → 개별 팩트카드 JSON (LLM 없음)
Step 2: 팩트카드 N개 → LLM → 트렌드/교차분석
Step 3: 트렌드 + 팩트카드 → LLM → 최종 기사
Step 4: 코드 → Validator → 사실/표현 검증 (LLM 없음)
```

#### 핵심 설계 요소

- **ALLOWED_PHRASES 매트릭스**: (상태 × 신뢰도) → 고정 문구. LLM이 팩트를 생성하지 않음
- **hira_evidence_type**: NON_REIMBURSED/NOT_FOUND 판정 근거 추적 (exact_price_match / board_missing / bridge_unmatched / source_not_loaded)
- **Validator 2단계 체크**: 하드(날짜/가격/상태 exact) + 소프트(문장 semantic 허용)
- **자동 교정 금지**: 위반 시 문단 폐기 → fallback fact-only 삽입 → 로그. silent corruption 방지

#### 설계 문서

`docs/design/briefing_pipeline_v2.md` — 전체 스키마, 데이터 흐름, 마이그레이션 계획

---

### 2. [RegScan] 4/15 세션 마무리

#### 완료 항목

| 항목 | 상태 |
|------|------|
| Decomposer v1.0.0 Logic Freezing | 완료 |
| OMOP RxNorm 교차 검증 | 완료 |
| HIRA enrichment + IngredientBridge fallback | 완료 |
| Few-shot HIRA 3패턴 + 네거티브 예시 | 완료 |
| 상태 엔진 v2 (7단계 HIRA + 5단계 MFDS + confidence + 가드레일) | 완료 |
| biz.hira.or.kr 약가 자동 다운로드 (Nexacro SSV + XHR 인터셉트) | 완료 |
| drug_price_collector 전면 재작성 | 완료 |
| dashboard PostgreSQL 지원 | 완료 |
| Docker DEV/PROD 분리 | 완료 |
| MFDS→식약처, HIRA→심평원 표기 통일 | 완료 |
| 약물 클릭 링크 + 출처 섹션 복원 | 완료 |
| v5.0~v5.5 브리핑 품질 반복 테스트 | 완료 |
| 비용 산정 | 완료 (GPT-5.2 월 ~46,000원) |

#### 미해결 — V2 파이프라인으로 이관

| 항목 | 우선순위 |
|------|---------|
| Unified briefing "본문 없음" 결함 | P0 → V2 Step 2-3에서 해결 |
| 팩트 검증 레이어 | P1 → V2 Step 4 |
| 기준일 본문 노출 | P1 → V2 Step 1 (FactCard.source_date) |
| 3층 출력 구조 | P1 → V2 전체 |
| MFDS 전수 조회 최적화 (45분→20초) | P2 |
| Cloud Run 배포 | P1 — V2 완성 후 |

---

## 관련 파일

| 파일 | 변경 |
|------|------|
| `docs/design/briefing_pipeline_v2.md` | **신규** — V2 파이프라인 전체 설계 문서 |
| `docs/review/2026-04-15_quality_review.md` | 품질 리뷰 리포트 |

---

### 3. [RegScan] Step 1 구현 완료 — `fact_card.py` (P0)

#### 기존 소스와의 차이점

| 항목 | 기존 (`briefing.py`) | 신규 (`fact_card.py`) |
|------|---------------------|---------------------|
| **팩트 생성 주체** | LLM이 drug dict를 보고 문장 생성 | **코드가 ALLOWED_PHRASES에서 선택** |
| **HIRA 상태 분기** | `_enrich_via_bridge()`에서 if/elif 4개로 fact 문자열 조합 | **HIRA_PHRASES 딕셔너리**: (상태, 신뢰도) → 고정 문구. 10개 조합 |
| **MFDS 판정** | `mfds_status == "미허가"`면 그대로 LLM에 전달 → LLM이 "미허가" 단정 | **MFDS_PHRASES**: 허가일 없으면 "허가 여부 추가 확인 필요" (단정 불가) |
| **FDA 시제** | few-shot으로 LLM에 위임 (hallucination 위험) | **FDA_PHRASES**: date vs today 비교 → "승인 완료"/"승인 예정" 코드 확정 |
| **가드레일** | `is_guardrailed` bool + `guardrail_note` 문자열 | **guardrail_notes 리스트**: HIRA/MFDS 각각 근거 명시 |
| **evidence_type** | 없음 | **hira_evidence_type**: exact_price_match / board_missing / bridge_unmatched / source_not_loaded |
| **출력 형태** | `hira_data` dict를 drug_dict에 주입 → `_extract_drug_intel()`이 20필드 추출 → LLM에 전달 | **FactCard dataclass**: `to_compact_dict()` (~10줄), `all_fact_phrases` (검증용), `hard_check_values` (날짜/가격) |
| **LLM 의존도** | 팩트 판정 + 기사 작성 동시 | **팩트 판정 0% LLM** — 코드가 전부 결정 |

#### 핵심 변화

1. **ALLOWED_PHRASES 매트릭스** — 모든 팩트 문구가 딕셔너리에서 나옴. 코드에 없는 문구는 나올 수 없음
2. **"미허가" 단정 제거** — 허가일 없는 "미허가"는 "허가 여부 추가 확인 필요"로 자동 완화
3. **hira_evidence_type** — NOT_FOUND / NON_REIMBURSED 판정 근거 추적 가능
4. **to_compact_dict()** — LLM 입력이 약물당 10줄로 압축 (기존: 20필드 raw dict)
5. **hard_check_values** — Validator(Step 4)에서 날짜/가격 exact match 검증용

#### 테스트 결과

| 약물 | MFDS (기존) | MFDS (신규) | 변화 |
|------|-----------|-----------|------|
| PEMBROLIZUMAB | 식약처 허가 | 식약처 허가 완료 (2015-03-25) | 날짜 추가 |
| BELUMOSUDIL | **식약처 미허가** | **허가 여부 추가 확인 필요** | 단정 → 확인 필요 |
| UNKNOWN_DRUG | (없음) | 허가 여부 추가 확인 필요 | 명시적 불확실성 |

---

## 관련 파일

| 파일 | 변경 |
|------|------|
| `regscan/stream/fact_card.py` | **신규** — Step 1 팩트카드 생성기 |
| `docs/design/briefing_pipeline_v2.md` | **신규** — V2 전체 설계 문서 |

---

### 4. [RegScan] Step 2 구현 완료 — `trend_analyzer.py`

- 팩트카드 N개 → LLM → 트렌드/교차분석/핵심 인사이트
- GPT → Gemini → Anthropic 3단 fallback 체인
- LLM 실패 시 코드 기반 `_fallback_trends` 자동 전환
- 품질 개선: Gemini 동기 SDK → `run_in_executor`, except 범위 축소, JSON 파싱 실패 로깅

| 커밋 | 내용 |
|------|------|
| `5e9e813` | feat(pipeline-v2): Step 2 — trend_analyzer.py |

---

### 5. [RegScan] Step 3 구현 완료 — `briefing.py` V2 리팩터

- `ENABLE_FACT_CARD_PIPELINE` 플래그로 V2/레거시 병렬 실행
- V2 시스템 프롬프트: "팩트카드 문구 verbatim 사용, 문장 연결만"
- V2 스트림별 프롬프트 4종 + V2 메서드 4개 + 공통 헬퍼 2개
- `_call_llm`에 optional `system_prompt` 파라미터 추가
- 품질 개선: `_count_designations` 헬퍼 추출, unified 배치 enrichment, `today` 파라미터화
- 기존 레거시 코드 전량 보존, 출력 JSON 스키마 동일

| 커밋 | 내용 |
|------|------|
| `4e8b8b0` | feat(pipeline-v2): Step 3 — briefing.py 리팩터 |

---

### 6. [RegScan] Step 4 구현 완료 — `fact_validator.py`

- 하드 체크: 날짜/가격/상태 exact match, 가드레일 단정문 금지
- 소프트 체크: 팩트 문구 키워드 포함 여부 (누락 시 로그만)
- 위반 처리: 문단 폐기 → fallback fact-only 삽입 (자동 교정 금지)
- 품질 개선: `copy.deepcopy`, field_path 설정 순서, 스키마 키 모듈 상수 추출

| 커밋 | 내용 |
|------|------|
| `40c21ee` | feat(pipeline-v2): Step 4 — fact_validator.py |

---

### 7. [RegScan] Step 3→4 연결 + E2E 검증

- briefing.py V2 메서드 4곳에 `_v2_validate()` 호출 연결
- `_hard_check_status`: IGNORECASE 적용 (case-sensitivity 버그 수정)
- `_hard_check_guardrail`: 팩트카드 자체 문구는 허용 (false positive 제거)
- E2E 테스트: Good=0 fallback / Bad=6 hard violations + 2 fallback

| 커밋 | 내용 |
|------|------|
| `bb1dc10` | feat(pipeline-v2): Step 3→4 Validator 연결 + 가드레일 버그 수정 |

---

## 관련 파일 (전체)

| 파일 | 변경 |
|------|------|
| `regscan/stream/fact_card.py` | **신규** — Step 1 팩트카드 생성기 |
| `regscan/stream/trend_analyzer.py` | **신규** — Step 2 트렌드 분석기 |
| `regscan/stream/briefing.py` | **수정** — Step 3 V2 Article Writer 리팩터 |
| `regscan/stream/fact_validator.py` | **신규** — Step 4 팩트 검증기 |
| `regscan/config/settings.py` | **수정** — `ENABLE_FACT_CARD_PIPELINE` 플래그 추가 |
| `docs/design/briefing_pipeline_v2.md` | **신규** — V2 전체 설계 문서 |

## V2 파이프라인 완성 현황

| Step | 파일 | 상태 |
|------|------|------|
| Step 1 | `fact_card.py` — 팩트카드 생성기 | ✅ 완료 |
| Step 2 | `trend_analyzer.py` — 트렌드 분석기 | ✅ 완료 |
| Step 3 | `briefing.py` — Article Writer 리팩터 | ✅ 완료 |
| Step 4 | `fact_validator.py` — 팩트 검증기 | ✅ 완료 |
| 연결 | Step 3→4 Validator 호출 | ✅ 완료 |
| E2E | 합성 데이터 검증 | ✅ 통과 |

## 다음 할 일

1. ~~Step 1: fact_card.py~~ ✅
2. ~~Step 2: trend_analyzer.py~~ ✅
3. ~~Step 3: briefing.py 리팩터~~ ✅
4. ~~Step 4: fact_validator.py~~ ✅
5. ~~Step 3→4 연결 + E2E~~ ✅
6. 프로덕션 배치 실행 (실제 LLM 호출 포함) → 결과 확인
7. 레거시 코드 제거 (Phase C) — 프로덕션 검증 후
8. Cloud Run 배포
