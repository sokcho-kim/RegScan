# 작업일지 - 2026-04-16

## 작업 내역

### 1. [RegScan] 브리핑 파이프라인 V2 설계 확정

#### 배경

4/15 배치 파이프라인 실행 결과, unified briefing에서 "브리핑 본문은 '없음'이지만..." 생성.
근본 원인: LLM이 팩트 생성 + 기사 작성을 동시에 하는 구조적 결함.
가드레일/프롬프팅으로 해결 불가 — 파이프라인 재설계 필요.

#### 확정된 4-Step 구조

```
Step 1: 코드 → 개별 팩트카드 JSON (LLM 없음)
Step 2: 팩트카드 N개 → LLM → 트렌드/교차분석
Step 3: 트렌드 + 팩트카드 → LLM → 최종 기사
Step 4: 코드 → Validator → 사실/표현 검증 (LLM 없음)
```

#### 핵심 설계 요소

- **ALLOWED_PHRASES 매트릭스**: (상태 × 신뢰도) → 고정 문구. LLM이 팩트를 생성하지 않음
- **hira_evidence_type**: NON_REIMBURSED/NOT_FOUND 판정 근거 추적 (exact_price_match / board_missing / bridge_unmatched / source_not_loaded)
- **Validator 2단계 체크**: 하드(날짜/가격/상태 exact) + 소프트(문장 semantic 허용)
- **자동 교정 금지**: 위반 시 문단 폐기 → fallback fact-only 삽입 → 로그. silent corruption 방지

#### 설계 문서

`docs/design/briefing_pipeline_v2.md` — 전체 스키마, 데이터 흐름, 마이그레이션 계획

---

### 2. [RegScan] 4/15 세션 마무리

#### 완료 항목

| 항목 | 상태 |
|------|------|
| Decomposer v1.0.0 Logic Freezing | 완료 |
| OMOP RxNorm 교차 검증 | 완료 |
| HIRA enrichment + IngredientBridge fallback | 완료 |
| Few-shot HIRA 3패턴 + 네거티브 예시 | 완료 |
| 상태 엔진 v2 (7단계 HIRA + 5단계 MFDS + confidence + 가드레일) | 완료 |
| biz.hira.or.kr 약가 자동 다운로드 (Nexacro SSV + XHR 인터셉트) | 완료 |
| drug_price_collector 전면 재작성 | 완료 |
| dashboard PostgreSQL 지원 | 완료 |
| Docker DEV/PROD 분리 | 완료 |
| MFDS→식약처, HIRA→심평원 표기 통일 | 완료 |
| 약물 클릭 링크 + 출처 섹션 복원 | 완료 |
| v5.0~v5.5 브리핑 품질 반복 테스트 | 완료 |
| 비용 산정 | 완료 (GPT-5.2 월 ~46,000원) |

#### 미해결 — V2 파이프라인으로 이관

| 항목 | 우선순위 |
|------|---------|
| Unified briefing "본문 없음" 결함 | P0 → V2 Step 2-3에서 해결 |
| 팩트 검증 레이어 | P1 → V2 Step 4 |
| 기준일 본문 노출 | P1 → V2 Step 1 (FactCard.source_date) |
| 3층 출력 구조 | P1 → V2 전체 |
| MFDS 전수 조회 최적화 (45분→20초) | P2 |
| Cloud Run 배포 | P1 — V2 완성 후 |

---

## 관련 파일

| 파일 | 변경 |
|------|------|
| `docs/design/briefing_pipeline_v2.md` | **신규** — V2 파이프라인 전체 설계 문서 |
| `docs/review/2026-04-15_quality_review.md` | 품질 리뷰 리포트 |

## 다음 할 일

1. Step 1: `regscan/stream/fact_card.py` 구현 (ALLOWED_PHRASES + FactCard + generate_fact_card)
2. Step 2: `regscan/stream/trend_analyzer.py` 구현
3. Step 3: `briefing.py` 리팩터
4. Step 4: `regscan/stream/fact_validator.py` 구현
5. 마이그레이션 + 테스트
