# 작업일지 - 2026-02-23

## 작업 내역

### LLM 기사 재생성 (프롬프트 v3.0 적용) — 49건 완료

2026-02-20 작업일지의 "다음 할 일" 중 **LLM 재생성** 진행. 동적 헤딩 + 마크다운 표/불릿이 실제 기사에 반영됨.

---

### 1. JSON 파싱 실패 수정 (`response_format`)

**문제**: gpt-4o-mini가 JSON을 코드 펜스(` ```json `)로 감싸서 반환 → `_parse_json_response()`에서 닫는 ` ``` ` 없어 파싱 실패 → 전체 응답이 `global_section`에 덤프됨. 5건 중 2건(Zanidatamab, Zolbetuximab) 실패.

**수정**:
- OpenAI API 호출에 `response_format={"type": "json_object"}` 추가 → JSON 출력 강제
- `_parse_json_response()` 다단계 파싱 추가 (코드 펜스 → 원본 → 브레이스 매칭)

**결과**: 재생성 후 파싱 실패 **0건**

---

### 2. LLM 호출 병렬화 (`asyncio.to_thread`)

**문제**: `asyncio.gather`로 병렬화 시도했으나, OpenAI 동기 클라이언트(`openai.OpenAI`)가 이벤트 루프를 블로킹 → 실질적으로 순차 실행됨.

**수정**:
- `_call_llm()` → `asyncio.to_thread(self._call_llm_sync, prompt)` 래핑
- 동기 API 호출을 스레드풀에서 실행 → `asyncio.gather` + `Semaphore(10)`으로 진짜 병렬화
- `publish_articles.py`의 메인 루프를 `_gen_one()` 코루틴 + `gather` 패턴으로 리팩터

**효과**: 49건 기준 ~18분(순차) → ~3분(병렬 10건) 예상 (다음 실행에서 확인)

---

### 3. 49건 기사 재생성 완료

| 항목 | 수치 |
|------|------|
| LLM 생성 | 49건 |
| 파싱 실패 | 0건 |
| 생성 실패 | 0건 |
| 소요 시간 | ~18분 (순차, 병렬화 이전) |
| 모델 | gpt-4o-mini |

**프롬프트 v3.0 적용 확인**:
- 동적 헤딩: "R-CHOP 실패 후의 치료 공백을 ADC가 메운다" (Polatuzumab 예시)
- 마크다운 표: 승인 기관/일자/적응증 비교표 포함
- 불릿 리스트: 경쟁약 현황, 국내 상태 구조화
- 경쟁약 분석: 동일 적응증 약물 간 포지셔닝 비교

---

### 4. 기사 팩트 에러 3건 구조적 수정

49건 재생성 후 팩트 체크에서 발견된 3가지 구조적 결함을 Python 사전 계산으로 해결.

#### Fix 1: 산정특례 시나리오 매핑 (medclaim 할루시네이션 방지)

**문제**: 비만약(semaglutide)에 암환자 산정특례 5% 시나리오가 삽입됨. 프롬프트가 모든 약물에 산정특례를 요구하고, few-shot이 전부 항암제 → LLM이 모든 약에 산정특례 적용.

**수정**:
- `publish_articles.py`: `COPAY_EXEMPTION_MAP` + `_get_copay_exemption()` 함수 추가
  - oncology → 암환자 산정특례(5%), rare_disease/희귀 → 희귀질환(10%), 그 외 → `null`
  - impact 객체에 `_copay_exemption` 주입
- `llm_generator.py`: `_prepare_drug_data()`에 `copay_exemption` 필드 추가
- `prompts.py`: "copay_exemption이 null이면 산정특례 절대 포함 금지" 규칙 추가, few-shot 예시에 `copay_exemption` 필드 추가

**검증 결과**:

| 약물 | therapeutic_area | copay_exemption | medclaim 산정특례 | 판정 |
|------|-----------------|-----------------|-------------------|------|
| semaglutide | metabolic | `null` | **없음** | ✅ 수정됨 |
| cabozantinib | oncology | 암환자 5% | 암환자 5% | ✅ |
| zanidatamab | oncology | 암환자 5% | 암환자 5% | ✅ |
| setmelanotide | metabolic+희귀 | 희귀질환 10% | 희귀질환 10% | ✅ |

#### Fix 2: 경쟁약 필터링 정밀화 (cabozantinib ↔ semaglutide 오매칭 방지)

**문제**: `CABOZANTINIB`의 areas = `"oncology,metabolic"` → metabolic 인덱스에도 등록 → semaglutide(metabolic)의 경쟁약으로 cabozantinib(항암제)이 매칭됨.

**수정**:
- `publish_articles.py`: `area_index` 구축 시 **primary area(첫 번째)만** 등록
  - 기존: 모든 area에 등록 (oncology에도 metabolic에도)
  - 변경: primary area에만 등록 (cabozantinib → oncology에만)
- 5-b 폴백 매칭에서 `comp_primary == target_primary` 조건 강화
- `prompts.py`: "없으면 당신이 아는 약물을 언급하라" → "competitors 없으면 경쟁약 비교 생략하라" 변경

**검증 결과**: semaglutide 경쟁약에 cabozantinib **미등장** ✅

#### Fix 3: 타임라인 사전 계산 (날짜/시제 오류 방지)

**문제**: EMA 승인일 2025-06-27(과거)을 "승인 예정"으로 표현. LLM이 날짜의 미래/과거를 판단하지 못함.

**수정**:
- `llm_generator.py`: `_build_approval_timeline()` 메서드 추가
  - 각 기관별 `{agency, status, date, tense}` 반환
  - tense: `"past"` / `"future"` / `"unknown"` — Python이 사전 계산
- `_prepare_drug_data()`에 `approval_timeline` 필드 주입
- `prompts.py`: "approval_timeline의 tense 필드를 반드시 따르라, 자체 날짜 판단 금지" 규칙 추가

**검증 결과**: gpt-4o-mini는 여전히 시제 오류 (아래 모델 평가에서 해결)

---

### 5. LLM 모델 평가 및 gpt-5.2 채택

Fix 3의 시제 오류가 gpt-4o-mini의 instruction-following 한계에서 비롯됨을 확인하고, 5개 모델을 비교 평가.

#### 평가 대상

zanidatamab-hrii 1건으로 통일 테스트 (EMA 2025-06-27 = 과거, 시제 판별이 핵심).

#### 1차 평가: gpt-4o-mini / gpt-5-mini / gpt-5-nano / gpt-5.2

| 모델 | 소요시간 | JSON 파싱 | EMA 시제 | 품질 |
|------|---------|-----------|---------|------|
| gpt-4o-mini | 20s | ✅ | ❌ "승인 예정" | 보통 |
| gpt-5-mini | 53s | ❌ 실패 | - | 사용불가 |
| gpt-5-nano | 23s | ❌ 실패 | - | 사용불가 |
| **gpt-5.2** | **30s** | **✅** | **✅ "승인 완료"** | **우수** |

gpt-5-mini/nano는 한국어 프롬프트 + JSON 구조화 출력을 감당하지 못함.

#### 2차 평가: gpt-5.2 vs gpt-5.2-pro

| 차원 | gpt-5.2 | gpt-5.2-pro |
|------|---------|-------------|
| **소요시간** | 30s | 116s (3.9x) |
| **API** | Chat Completions | Responses API 전용 |
| **시제 정확도** | ✅ 완벽 | ✅ 완벽 |
| **산정특례** | ✅ | ✅ |
| **headline** | "HER2 담도암 2차 표적, zanidatamab—미허가 공백" | "HER2 이중항체 자니다타맙, 담도암 2차 FDA·EMA 승인" |
| **실무 가이드 깊이** | 높음 | 매우 높음 (병리 결과지 템플릿 등) |
| **경쟁약 분석** | 데이터 기반 정직한 분석 | + 도메인 지식(trastuzumab deruxtecan) 추가 |

#### 비용 비교 (49건 기준)

| | gpt-4o-mini | gpt-5.2 | gpt-5.2-pro |
|---|---|---|---|
| **가격 (input/1M)** | $0.15 | $1.75 | ~$15+ |
| **가격 (output/1M)** | $0.60 | $14.00 | ~$60+ |
| **49건 input (0.37M)** | $0.06 | $0.65 | ~$5.6 |
| **49건 output (0.12M)** | $0.07 | $1.71 | ~$7.4 |
| **49건 총액** | **$0.13** | **$2.36** | **~$13+** |
| **49건 소요시간 (병렬10)** | ~2분 | ~5분 | ~20분+ |

> 가격 출처: [OpenAI API Pricing](https://openai.com/api/pricing/)

#### 채택 결정: **gpt-5.2**

- 시제/산정특례/경쟁약 3대 팩트 에러 **모두 해결**
- 49건 재생성 비용 **~$2.4** (gpt-4o-mini 대비 18배이지만 절대금액 미미)
- 소요시간 **~5분** (병렬 10건 기준, 반나절 아님)
- gpt-5.2-pro 대비 비용 1/5, 속도 4배, 정확도 동일

---

## 수정 파일 요약

| 파일 | 변경 내용 |
|------|----------|
| `regscan/report/llm_generator.py` | `response_format` 추가, 다단계 파싱, 병렬화, **`_build_approval_timeline()` 추가**, **`copay_exemption`/`approval_timeline` 주입**, **기본 모델 gpt-5.2로 변경** |
| `regscan/scripts/publish_articles.py` | 병렬 패턴 리팩터, **`COPAY_EXEMPTION_MAP` + `_get_copay_exemption()` 추가**, **area_index primary area only**, **기본 모델 gpt-5.2로 변경** |
| `regscan/report/prompts.py` | **산정특례 조건부 규칙 추가**, **경쟁약 hallucination 지시 삭제**, **타임라인 시제 규칙 추가**, **few-shot에 copay_exemption 필드 추가** |

---

## 결과물

- `output/briefings/*.html` — 49건 HTML 기사 (v3.0 프롬프트 적용)
- `output/briefings/*.json` — 메타데이터 보강된 JSON
- `output/briefings/index.html` — 인덱스 페이지
- `output/briefings/hot_issues_2026-02-23.json` — 핫이슈 메타
- `output/briefings/test_5.2_vs_pro.json` — 모델 비교 테스트 결과

---

## 이슈

- **DB 중복 INN**: `POLATUZUMAB VEDOTIN`(score 60)과 `polatuzumab vedotin`(score 50)이 별도 레코드로 존재 → 2건 중복 생성. 다른 약물도 동일 패턴 (대소문자만 다른 중복).
- **파일명 불일치**: 구 파일(`POLATUZUMAB_VEDOTIN-PIIQ.json`)과 신 파일(`polatuzumab_vedotin.json`)이 공존. `_safe_filename()`이 `inn.lower()` 사용하므로 USAN 접미사(-PIIQ) 유무에 따라 달라짐.
- **gpt-5.2-pro API 제약**: Chat Completions 미지원, Responses API 전용. `llm_generator.py`에서 사용하려면 별도 분기 필요.

---

## 다음 할 일

- [ ] **gpt-5.2로 49건 전체 재생성** (비용 ~$2.4, 소요 ~5분)
- [ ] 재생성 후 5건 수동 팩트 체크 (semaglutide, cabozantinib, zanidatamab, setmelanotide, polatuzumab)
- [ ] DB INN 중복 정리 (대소문자 정규화, USAN 접미사 통일)
- [ ] 구 파일명 정리 (중복 HTML/JSON 삭제)
- [ ] 임상시험명 블랙리스트 추가 검토 (KEYNOTE, DESTINY, CHECKMATE 등)
- [ ] 배포 CRITICAL 조치: `.dockerignore` 생성, API 키 로테이션, CI/CD 구축
- [ ] medRxiv 연결 안정화 (chunked read timeout/retry)
- [ ] EMA EPAR에서 MOA 텍스트 추출
- [ ] 약가 시뮬레이션 (동일 area 기존 급여 약물 가격 통계 기반)
