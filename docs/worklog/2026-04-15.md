# 작업일지 - 2026-04-15

## 작업 내역

### 1. [RegScan] OMOP RxNorm 기반 Decomposer 사전 검증 스크립트 작성 + 실행 (P2 — 완료)

#### 1-1. 배경

4/14에 구현한 `decomposer.py`의 3개 사전(`SALT_FORMS`, `REF_NORMALIZATION_MAP`, `FORMULATION_TOKENS`)은 MFDS 마스터 CSV 기반 임의 정의.
OMOP CDM의 RxNorm vocabulary(Athena 다운로드)로 교차 검증하여 누락/오류를 보정하기로 한 계획의 실행.

#### 1-2. 스크립트

- **파일**: `scripts/validate_decomposer_vocab.py`
- **데이터 소스**: `data/omop/CONCEPT.csv` (838MB, 6.4M rows)
- **처리 방식**: 스트리밍 1-pass로 RxNorm Ingredient(36,356건) / Precise Ingredient(3,658건) / Dose Form(258건) 추출 후, PI 이름에서 Ingredient 이름을 빼서 salt 토큰을 역추출

```
Usage: python scripts/validate_decomposer_vocab.py
```

#### 1-3. 검증 결과 요약

##### SALT_FORMS (현재 102개)

| 구분 | 건수 | 비고 |
|------|------|------|
| RxNorm 확인 (공통) | 81개 (79%) | 우리 사전의 대부분이 RxNorm에도 존재 |
| RxNorm 누락 후보 | 841개 (전체) | 빈도 ≥ 3인 것 45개 |
| 우리에만 있음 | 21개 | 마스터 고유, 삭제 불필요 |

**RxNorm 누락 후보 상위 (빈도순)**:

| Salt | 빈도 | 판정 |
|------|------|------|
| `chloride` | 57 | 염이 아닌 화합물명 일부 (sodium chloride 등) — 추가 불필요 |
| `bromide` | 34 | 동일 — 화합물명 자체 |
| `disodium` | 19 | **이미 REF_NORMALIZATION_MAP에 있음** (disodium → sodium) |
| `hexahydrate` | 12 | 수화물 변이 — hydrate 계열 확장 검토 가능 |
| `polistirex` | 7 | 서방형 수지 복합체 — salt보다 제형에 가까움 |
| `acetonide` | 3 | 스테로이드 에스테르 — **추가 후보** |
| `pivalate` | 5 | 에스테르 — **추가 후보** |
| `dipropionate` | 5 | 에스테르 — **추가 후보** |
| `myristate` | 5 | 지방산 에스테르 — **추가 후보** |
| `methylsulfate` | 6 | 유기황산염 — **추가 후보** |

**우리에만 있는 21개 (RxNorm 미등재)**:
`aluminium`, `ammonium`, `bicarbonate`, `diethanolamine`, `ethanolamine`, `gentisate`, `hemioxalate`, `hemisuccinate`, `hemitartrate`, `heptanoate`, `hexanoate`, `isobutyrate`, `isovalerate`, `lithium`, `napadisylate`, `phthalate`, `subacetate`, `subsalicylate`, `triacetate`, `triflutate`, `trihydrochloride`
→ RxNorm은 미국 중심(USP), 우리 마스터는 글로벌 INN 기반이므로 차이는 자연스러움. 삭제 불필요.

##### REF_NORMALIZATION_MAP (현재 21개 치환 규칙)

| Key (변이형) | Value (표준형) | Key in RxNorm | Value in RxNorm |
|-------------|---------------|:---:|:---:|
| besylate | besilate | OK | OK |
| hcl | hydrochloride | OK | OK |
| disodium | sodium | OK | OK |
| dimeglumine | meglumine | OK | OK |
| trisodium | sodium | OK | OK |
| trometamol | tromethamine | OK | OK |
| olamine | ethanolamine | OK | -- |
| 나머지 14개 | (표준형) | -- | OK |

- Key가 RxNorm에 있는 7개: RxNorm에서도 변이형을 실제 사용 → 치환 규칙 유효성 확인
- Value(표준형)는 거의 전부 RxNorm에 존재 — `ethanolamine`만 미등재 (RxNorm이 `olamine`을 그대로 사용)
- **조치**: `olamine → ethanolamine` 치환 방향 재검토 필요. RxNorm 기준이면 `ethanolamine → olamine`이 맞을 수 있으나, USP/INN 기준에서는 `ethanolamine`이 표준. 현행 유지.

##### FORMULATION_TOKENS (현재 29개)

| 구분 | 건수 |
|------|------|
| RxNorm Dose Form에서 확인 | 9개 (31%) |
| 마스터 고유 (RxNorm 미등재) | 20개 (69%) |

**RxNorm 확인된 9개**: `chewable`, `coated`, `delayed release`, `enteric coated`, `extended release`, `granule`, `granules`, `powder`, `sustained release`

**RxNorm에 없는 20개** (마스터 고유):
`micronized`, `liposomal`, `pegylated`, `nanocrystal`, `lyophilized`, `lyophilisate`, `concentrate`, `film coated`, `dispersible`, `controlled release`, `modified release`, `coated granules`, `coated powder`, `concentrate powder`, `concentrate granule(s)`, `enteric pellets`, `extended release microspheres`, `dried extract`, `soft extract`

→ RxNorm Dose Form은 "Oral Tablet", "Injectable Solution" 등 최종 제형 중심. 우리 토큰은 **원료 물리 형태**(micronized, granules)나 **가공 방식**(lyophilized, pegylated)이므로 분류 기준 자체가 다름. 누락이 아닌 체계 차이.

**RxNorm Dose Form에서 발견된 추가 후보 키워드**:
`effervescent`(2), `sublingual`(3), `implant`(1) — 마스터에서 실제 등장하는지 확인 후 추가 검토

#### 1-4. 핵심 인사이트

1. **SALT_FORMS 79% 유효** — 102개 중 81개가 RxNorm 검증됨. 나머지 21개는 INN/유럽 약전 기준 salt로 삭제 불필요
2. **추가 후보 5개 식별** — `acetonide`, `pivalate`, `dipropionate`, `myristate`, `methylsulfate` (스테로이드/지방산 에스테르 계열)
3. **REF_NORMALIZATION_MAP 유효** — 21개 치환 규칙 모두 표준형이 RxNorm에 존재 (1건 제외)
4. **FORMULATION_TOKENS는 체계 차이** — RxNorm Dose Form과 분류 기준이 근본적으로 다름. 우리 토큰은 원료 물리 형태 중심이므로 OMOP 대비 부족이 아닌 상호 보완 관계
5. **Precise Ingredient 미분해 845건/3,658건** (23%) — 대부분 바이오의약품(-mab, -nib 접미사가 그대로 PI명), 복합 salt, 방사성 동위원소(F-18 등) 

#### 1-5. 후속 조치

| 항목 | 우선순위 | 상태 |
|------|---------|------|
| SALT_FORMS에 추가 후보 5개 반영 | P3 | 미착수 — 마스터에서 실제 등장 확인 후 |
| `hexahydrate`, `tetrahydrate` 등 수화물 변이 확장 | P3 | 미착수 |
| `effervescent`, `sublingual` 등 제형 토큰 추가 검토 | P3 | 미착수 |
| PostgreSQL 스키마 확장 (base_inn, salt, formulation, strength) | P2 | 미착수 |
| 590건 미매칭 심층 분류 | P2 | 미착수 |

---

### 2. [RegScan] SALT_FORMS RxNorm 기반 확장 실험 + 구제율 검증 (P2 — 완료)

#### 2-1. 실험 설계

RxNorm에서 빈도 ≥ 3으로 검출된 45개 누락 salt 후보를 SALT_FORMS에 반영하고 구제율 변화를 측정.

#### 2-2. 1차 시도: 전수 투입 (실패)

45개 중 비-salt 제외 후 20개 + REF_NORMALIZATION_MAP 2개를 추가.

**결과: regression 발생** — unmatched +5건, decomposed_base_fallback -7건

**원인 분석** (7건 전수 역추적):
- `furoate` → "Fluticasone Furoate Micronized", "Mometasone Furoate Micronized" — furoate가 salt로 추출되어 base_key가 "fluticasone"으로 변경 → 마스터에 "fluticasone furoate"로만 등재되어 있어 매칭 실패
- `bromide` → "Aclidinium Bromide Micronized", "Tiotropium Bromide Hydrate" — 동일 패턴

**핵심 발견**: 할로겐화합물(chloride, bromide, iodide, fluoride) 및 특정 에스테르(furoate, dipropionate, pivalate, myristate, mononitrate)는 **약물명의 일부**이지 분리 가능한 염이 아님. SALT_FORMS에 넣으면 과잉 분해로 오히려 매칭률이 하락.

#### 2-3. 2차 시도: 안전 토큰만 선별 (성공)

과잉 분해 위험 9종 제거, 순수 염/에스테르/수화물 12종만 추가:

| 분류 | 추가 토큰 |
|------|----------|
| 유기산염 | `pidolate`, `adipate`, `cocoate`, `picrate` |
| 에스테르 | `acetonide`, `cypionate` |
| 유기황산염 | `methylsulfate` |
| 수지 복합체 | `polistirex` |
| 무기염 | `pyrophosphate` |
| 수화물 변이 | `hexahydrate`, `tetrahydrate`, `decahydrate` |

REF_NORMALIZATION_MAP 추가: `tetrasodium→sodium`, `monohydrochloride→hydrochloride`

#### 2-4. 최종 Before/After (고유 첫-성분 6,414건)

| 매칭 방법 | Before | After | Delta |
|-----------|--------|-------|-------|
| normalized | 4,569 | 4,569 | 0 |
| decomposed_base_fallback | 93 | 93 | 0 |
| decomposed_variant | 14 | **16** | **+2** |
| atc | 26 | 26 | 0 |
| herbal_detected | 937 | 937 | 0 |
| **unmatched** | **775** | **773** | **-2** |

- **Regression: 0건**
- **구제: +2건** (`Diquafosol Tetrasodium` — tetrasodium→sodium 치환)
- SALT_FORMS: 102개 → **114개**, REF_NORMALIZATION_MAP: 21개 → **23개**

#### 2-5. 미매칭 773건 상위 분석

| 순위 | 성분명 | 품목수 | 원인 |
|------|--------|--------|------|
| 1 | Oxygen | 103 | 의료용 가스 — HIRA 약가 아닌 별도 수가 |
| 2 | Metformin HCl with Colloidal Anhydrous Silica/Sitagliptin HCl Hydrate | 81 | 부형제(Colloidal Anhydrous Silica) 포함 복합제명 |
| 3 | Limaprost Alfadex | 80 | 포접체(cyclodextrin complex) — 마스터 미등재 |
| 4 | Itraconazole Solid Dispersions | 60 | 제형(Solid Dispersions)이 성분명에 포함 |
| 5 | Duloxetine HCl Enteric-Coated Granules | 48 | 제형(Enteric-Coated Granules)이 성분명에 포함 |

#### 2-6. 결론

사전 확장의 구제 효과는 미미(+2건). 진짜 수확:
1. **SALT_FORMS 79% 유효성 확인** — 임의 정의 → RxNorm 교차 검증된 사전
2. **과잉 분해 위험군 9종 식별** — halide/ester를 salt로 넣으면 안 된다는 실증
3. **미매칭 본질 파악** — 코드 로직이 아닌 마스터 데이터 범위의 한계. 억지 퍼지매칭은 데이터 품질 저하

---

### 3. [RegScan] Logic Freezing + 스키마 확장 + 사전 확장 + 미매칭 리포트 (P2 — 완료)

#### 3-1. Logic Freezing

- `decomposer.py`에 `__version__ = "1.0.0"` 선언
- 상수 3종을 `regscan/map/assets/*.json`으로 분리 → `assets_loader.py`에서 최초 1회 로드 + frozenset 캐싱 + regex 컴파일
- `tests/test_decomposer_snapshot.py` — 6,414건 전수 분해 골든 스냅샷 테스트 추가
- git tag `decomposer-v1.0.0` 생성

#### 3-2. SQL 스키마 확장

`hira_reimbursements` 테이블에 분해 결과 컬럼 6개 추가:
- `raw_ingredient` (VARCHAR 500) — MFDS 원본 성분명
- `base_inn` (VARCHAR 200, indexed) — 핵심 INN
- `salt` (VARCHAR 100) — 표준화 염
- `formulation` (VARCHAR 100) — 제형 variant
- `strength` (VARCHAR 50) — 함량
- `match_method` (VARCHAR 30) — 매칭 방법

#### 3-3. FORMULATION_TOKENS 사전 확장

MFDS 실데이터 등장 빈도 기반으로 7개 토큰 추가:
| 토큰 | MFDS 등장 | 비고 |
|------|----------|------|
| `solid dispersions` / `solid dispersion` | 63 | 고체분산체 |
| `enteric-coated` | 48 | 하이픈 변형 (기존 `enteric coated`만 있었음) |
| `alfadex` | 82 | α-cyclodextrin 포접체 |
| `betadex` | 1 | β-cyclodextrin 포접체 |
| `pellets` / `pellet` | 55 | 펠렛 제형 |

확장 결과: FORMULATION_TOKENS 29개 → **36개**, unmatched 773 → **768** (-5건), decomposed_base_fallback 93 → **98** (+5건), Regression 0건.

#### 3-4. 미매칭 리포트

768건 전수 분류 → `docs/unmatched_ingredient_report.md`

| 카테고리 | 고유 | 품목수 | 코드 해결 |
|----------|------|--------|----------|
| pure_unmatched (마스터 미등재) | 304 | 1,129 | X |
| complex_combo (부형제/복합) | 212 | 531 | X |
| extract (추출물) | 115 | 295 | X |
| formulation_in_name (제형 포함) | 27 | 280 | 완료 |
| gas (가스류) | 6 | 188 | X |
| blood_product (혈액제제) | 28 | 182 | X |
| vaccine_bio (백신/면역) | 60 | 90 | X |
| cell_therapy (세포치료) | 21 | 27 | X |

**결론**: 미매칭의 근본 원인은 코드 로직이 아닌 데이터 소스의 구조적 한계. Decomposer v1.0.0의 책임 범위를 확정.

---

## 관련 파일

| 파일 | 변경 |
|------|------|
| `scripts/validate_decomposer_vocab.py` | **신규** — OMOP RxNorm 교차 검증 스크립트 |
| `regscan/map/decomposer.py` | v1.0.0, 상수 제거 → assets_loader import |
| `regscan/map/assets_loader.py` | **신규** — JSON 로드 + frozenset 캐싱 + regex 컴파일 |
| `regscan/map/assets/*.json` | **신규** — SALT_FORMS(114), REF_NORMALIZATION_MAP(23), FORMULATION_TOKENS(36) |
| `regscan/db/models.py` | HIRAReimbursementDB에 분해 결과 컬럼 6개 추가 |
| `tests/test_decomposer_snapshot.py` | **신규** — 6,414건 전수 스냅샷 테스트 |
| `tests/fixtures/decomposer_snapshot.json` | **신규** — 골든 데이터 |
| `docs/unmatched_ingredient_report.md` | **신규** — 미매칭 768건 전수 분류 리포트 |

---

### 4. [RegScan] 브리핑 HIRA Enrichment + Few-shot 전략 + 품질 테스트 (P1 — 완료)

#### 4-1. HIRA Enrichment IngredientBridge Fallback

- `briefing.py`에 `_enrich_via_bridge()` 추가 — DB 없이 JSON 직접 조회
- `_get_bridge()` 싱글턴 로드 (마스터+ATC+약가 자동 탐색)
- 용량 규격 추출: `_extract_dosage_from_raw()` → "2,103,620원 (0.1g/4mL)"
- HIRA 상태 4단계: 급여 등재 / 비급여 / 급여 삭제 / 확인 자료 없음

#### 4-2. Few-shot HIRA 시맨틱 매핑

- Therapeutic few-shot: HIRA 3패턴 시연 (급여+산정특례 / 미등재+access_routes / 데이터없음=생략)
- Innovation few-shot: 급여/미등재 + access_routes 유무 분기
- 네거티브 few-shot: EXAMPLE_PENDING → "FDA 승인일 미공개"
- max_completion_tokens 2500→3500

#### 4-3. 품질 테스트 프레임워크

- `scripts/test_briefing_quality.py` — 실데이터 기반 브리핑 생성 + 버전별 스냅샷 저장
- 프롬프트 캡처: `_last_system_prompt`, `_last_user_prompt`, `_last_model_used`
- v5.0 (하드코딩 데이터, 팩트 오류 발견), v5.1 (실제 파이프라인), v5.2 (자동 top5)

#### 4-4. GPT 팩트체크 결과

- v5.0 기사 품질 5/10 — 테스트 데이터 하드코딩으로 인한 팩트 오류
- v5.2 기사 품질 7/10 — 자동 선별 + 실데이터, HIRA 라벨링 거침
- 개선: 용량 규격 추가, HIRA 4단계 상태, "미허가"/"미확인" 구분

---

### 5. [RegScan] HIRA biz.hira.or.kr 약가파일 다운로드 자동화 (P1 — 진행 중)

#### 5-1. Nexacro SSV 프로토콜 역공학

- SSV 구분자: RS=0x1e, US=0x1f, ETX=0x03
- `selectComBbsList.ndo` → 적용약가 게시글 자동 탐지 (제목, nttId, atchFileId)
- `selectComBbsFileList.ndo` → 첨부파일 메타 조회 (파일명, 크기, 경로)

#### 5-2. UI 네비게이션 자동화

- 메인 → 심사기준 종합서비스 → InfoBank 팝업 열기
- 키보드 ArrowDown으로 Nexacro 가상 스크롤 우회 → "청구관련기준(마스터파일)" 접근
- 적용약가 게시글 클릭 → 파일 리스트 표시 확인
- 체크박스 dispatchEvent → 다운로드 버튼 dispatchEvent → CDP 다운로드 이벤트 수신

#### 5-3. 현재 상태

- **모니터링 100% 자동화** — 최신 약가파일 존재 여부 + 기존 JSON과 비교
- **다운로드 90%** — CDP 다운로드 완료 이벤트는 받지만, Dext5가 blob/saveAs 방식이라 파일이 디스크에 안 떨어짐
- **남은 작업**: dext5handler.ndo XHR 응답 인터셉트 (`page.route`) 방식 테스트 필요

#### 5-4. Dext5 makeEncryptParam 역공학

```
base64( "R" + base64(원본) )  →  URL-safe (+→%2B)
```

---

### 6. [RegScan] README 전면 재작성 + 기타

- 아키텍처 다이어그램, 프로젝트 구조, 핵심 모듈, Docker, 환경 변수, 사전 갱신 주기
- git tag `decomposer-v1.0.0`

---

## 관련 파일 (4~6번)

| 파일 | 변경 |
|------|------|
| `regscan/stream/briefing.py` | HIRA enrichment fallback + few-shot 3패턴 + 프롬프트 캡처 + 토큰 상향 |
| `scripts/test_briefing_quality.py` | **신규** — 브리핑 품질 테스트 (버전별 저장) |
| `scripts/download_hira_yakga.py` | **신규** — HIRA 약가파일 모니터링 + UI 자동화 |
| `output/briefings/snapshots/v5.*` | 브리핑 버전별 스냅샷 (결과+프롬프트+입력+메타) |
| `docs/unmatched_ingredient_report.md` | 미매칭 768건 전수 분류 |
| `README.md` | 전면 재작성 |

---

### 7. [RegScan] HIRA 약가파일 다운로드 파이프라인 완성 + drug_price_collector 재작성 (P1 — 완료)

- `download_latest_drug_price()` 전면 재작성 → Nexacro SSV API + UI dispatchEvent + XHR 인터셉트
- `_extract_date_from_filename()` 버그 수정: `230401_260401` → `20260401` (마지막 날짜 사용)
- 전체 파이프라인 1줄 실행: `python -m regscan.workers.drug_price_collector`
  - Step 1: 다운로드 (43초, 9.1MB)
  - Step 2: Excel → JSON (59,076건)
  - Step 3: 변경 감지 (DataFrame 해시 + 레코드 diff)

### 8. [RegScan] 배포 준비 (P1 — 완료)

- `dashboard.py`: `stream_briefing_page` PostgreSQL 분기 추가 (`_load_briefings_pg`)
- `docker-compose.yml`: `env_file: .env` 추가, DEV/PROD 분리
- `docker-compose.prod.yml`: DB 포트 비노출, restart, WARNING 로그

### 9. [RegScan] v5.3 브리핑 품질 테스트 + 비용 산정 (P1 — 완료)

- 4월 최신 약가(`drug_prices_20260401.json`) 반영 후 브리핑 재생성
- HIRA 키워드 6/8 검출, 용량 규격 표기 정상
- v5.0→v5.3 개선: 팩트 오류 제거, 라벨링 정밀화, 실데이터 기반

#### 비용 산정

| 항목 | GPT-5.2 | Gemini 2.5 Flash |
|------|---------|-----------------|
| 단건 브리핑 | 34원 | 3원 |
| LLM 월 (일 4건) | 4,092원 | 307원 |
| 인프라 월 (Cloud Run + DB) | 42,000원 | 42,000원 |
| **월 합계** | **~46,000원** | **~42,000원** |

---

## 관련 파일 (7~9번 추가)

| 파일 | 변경 |
|------|------|
| `regscan/workers/drug_price_collector.py` | 전면 재작성 — biz.hira.or.kr 완전 자동화 |
| `regscan/api/routes/dashboard.py` | PostgreSQL 분기 추가 |
| `docker-compose.yml` | DEV/PROD 분리, env_file |
| `docker-compose.prod.yml` | **신규** — PROD 오버라이드 |
| `output/briefings/snapshots/v5.3_april_prices/` | 4월 약가 기반 브리핑 결과 |

## 커밋 요약 (오늘 — 총 20+커밋)

| Hash | 내용 |
|------|------|
| 8ad6644 | Decomposer 무손실 분해 + OMOP 검증 |
| dc4ad96 | assets JSON 분리 + 스냅샷 테스트 |
| 2a41946 | Logic Freezing + 스키마 확장 + 미매칭 리포트 |
| de24598 | HIRA 워커 통합 + KDCA 크롤러 |
| 2f84ad3 | HIRA enrichment IngredientBridge fallback |
| 2306393 | few-shot HIRA 시맨틱 매핑 + 네거티브 예시 |
| 09f5d58 | HIRA 라벨링 3종 개선 |
| 6438665 | README 전면 재작성 |
| fb4725b | drug_price_collector 전면 재작성 — 완전 자동화 |
| 7aedf7b | dashboard PostgreSQL 지원 |
| 37c24bb | DEV/PROD 환경 분리 |

## 미해결 과제

| 항목 | 우선순위 | 상태 |
|------|---------|------|
| Cloud Run 배포 (batch + web) | P1 | 미착수 |
| Tailscale + SSH 원격 환경 | P2 | 진행 중 |
