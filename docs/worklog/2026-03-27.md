# 작업일지 - 2026-03-27

## 작업 내역

### [RegScan] V3 브리핑 프롬프트 재설계 + E2E 테스트

V2 Executive Briefing 품질 이슈 8일째 미해결 → V3 프롬프트 전면 재설계.

#### 변경 파일: `regscan/stream/briefing.py` (8건)

1. **SYSTEM_PROMPT 템플릿화** — `{today}` 런타임 주입 + 시간추론 규칙 + GOOD/BAD few-shot 2쌍
2. **THERAPEUTIC_BRIEFING_PROMPT** — `why_it_matters` 4관점 가이드 (경쟁구도/급여/환자규모/처방변화)
3. **INNOVATION_BRIEFING_PROMPT** — `implication` 3관점 가이드 (혁신성/도입시점/포지셔닝)
4. **EXTERNAL_BRIEFING_PROMPT** — `hospital_impact` 3관점 (재고/대체약물/보험청구)
5. **UNIFIED_BRIEFING_PROMPT** — 스트림 브리핑 JSON 직접 주입 + `key_takeaway` 필드 추가 + "단순 반복 금지" 명시
6. **`_extract_drug_intel()`** — max_fields 8→14 (clinical_results, mfds_data, therapeutic_areas 추가)
7. **`_call_llm()`** — `SYSTEM_PROMPT.format(today=today)` 3개 LLM 모두 적용
8. **`generate_unified_briefing()`** — 스트림 브리핑 JSON 직접 전달 + 4000자 truncation + 스트림별 통계

#### 신규 파일: `tests/test_v3_briefing.py`

- 오프라인 6개: SYSTEM_PROMPT 구조, `_extract_drug_intel` V3 필드
- E2E 4개: 치료영역/혁신/외부시그널/통합 브리핑 (RILZABRUTINIB 단일 약물, gpt-5.2 실제 호출)
- 검증: key_takeaway 포함, 시제 정확성, why_it_matters 구체성, JSON 스키마 완전성

**결과: 10/10 PASSED** (86초)

## 이슈

### OpenAI gpt-5.2 `max_tokens` 파라미터 변경
- gpt-5.2가 `max_tokens` 미지원 → `max_completion_tokens`로 변경 필요
- 이 버그 때문에 OpenAI 호출이 400 에러로 실패, Gemini fallback → Gemini 쿼터 초과 시 전부 실패
- `_call_llm()`에서 `max_tokens` → `max_completion_tokens`로 수정하여 해결

### pytest-asyncio 버전 문제
- 기존 1.3.0 설치됨 → 0.25.3으로 업그레이드 필요 (uv 사용)
- `uv pip install "pytest-asyncio==0.25.3"`

## 다음 할 일
- 대시보드 `/dashboard/briefing` 렌더링 검증
- 다른 약물로 추가 E2E 검증 (미래 승인 예정 약물 → 미래 시제 검증)
- Gemini 쿼터 회복 후 fallback 체인 정상 동작 확인
