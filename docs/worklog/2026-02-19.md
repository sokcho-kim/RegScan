# 작업일지 - 2026-02-19

## 작업 내역

### v3 파이프라인 버그 6건 수정 + 파이프라인 실행

2026-02-13 작업일지의 "다음 할 일" 6건을 전부 처리.

---

### 1. DBLoader raw_data 저장 구현

**문제**: `RegulatoryEventDB.raw_data` 컬럼이 975건 전부 NULL. `DomesticImpact` 데이터클래스에 raw_data 필드가 없어 데이터 흐름 중간에 유실.

**수정 파일**:

| 파일 | 변경 |
|------|------|
| `regscan/scan/domestic.py` | `DomesticImpact`에 `fda_raw_data`, `ema_raw_data`, `mfds_raw_data` 필드 추가 + `analyze()`에서 복사 |
| `regscan/db/loader.py` | `_upsert_events()`, `_upsert_events_with_changes()`에서 raw_data 전달 |

**결과**: raw_data non-NULL 0건 → **890건** (69% 커버리지)

---

### 2. therapeutic_areas / stream_sources DB 반영 확인

기존 구현이 이미 정상 작동 확인. 추가 코드 변경 없음.
- therapeutic_areas: 688/818 → 797/1215
- stream_sources: 1116/1215 (92%)

---

### 3. Stream 3 ↔ 기존 DB 교차 매칭 (INN 유실 수정)

**문제 3가지**:
1. `build_global_statuses()`에서 Stream 3-only 약물의 INN 유실 (`build_from_fda_ema(None, None)` → INN 빈 값)
2. score=0 약물이 `MIN_SCORE_FOR_DB=10` 필터에 걸려 DB 미저장
3. `StreamResult.signals`가 개별 약물에 연결되지 않음

**수정 파일**: `regscan/stream/orchestrator.py`
- INN 보존: merge 결과에서 status로 INN 복사
- CT.gov Phase 3 시그널 → 최소 10점 (실패 시 15점) 자동 부여
- `merge_results()`에서 INN 기반 signal→drug 연결 추가

---

### 4. FDA Breakthrough 404 수정

**근본 원인**: openFDA `drugsfda.json`에 Breakthrough Therapy Designation 데이터 자체가 없음. `submission_class_code`에는 텍스트 값(`"TYPE 1"`, `"TYPE 3"`, `"EFFICACY"` 등)만 존재.

**수정 파일**: `regscan/stream/innovation.py`
- NME: `["1", "TYPE 1"]` 반복 → `'"TYPE 1"'` (올바른 쿼팅)
- Breakthrough: 404 나던 코드 제거 → `"TYPE 5"` (Accelerated Approval) + `"TYPE 4"` (Priority Review) 대체

---

### 5. 복합성분명(세미콜론) FDA 검색 수정

**문제**: `pertuzumab;trastuzumab` 같은 복합성분명으로 FDA API 호출 시 400 Bad Request.

**수정 파일**: `regscan/stream/therapeutic.py`
- `_enrich_with_fda_crossref()`에서 세미콜론(`;`), 슬래시(` / `) 분리 후 개별 성분명으로 검색
- 첫 번째 성분 검색 성공 시 break

---

### 6. 주간 브리핑 데이터 소스 확인

기존 구현이 이미 활성화 확인. 추가 코드 변경 없음.
- FDA Safety (Boxed Warning + Enforcement): `ENABLE_FDA_SAFETY=True`
- FDA AdCom: `ENABLE_FDA_ADCOM=True`
- PDUFA 일정: DB 기반 조회 구현 완료

---

## 파이프라인 실행 결과

**run_id**: `33d32187-c4ab-44d5-8154-eadb9e65eda1`
**소요 시간**: 1134초 (~19분)

### 스트림별 수집

| 스트림 | 수집 결과 |
|--------|----------|
| Stream 1 (Therapeutic) | FDA pharm_class 103건 + EMA 258건 (oncology), 나머지 영역 포함 |
| Stream 2 (Innovation) | NME + Accelerated/Priority + PRIME + Orphan + Conditional |
| Stream 3 (External) | CT.gov Phase 3: 163건, medRxiv: 0건 (연결 오류) |

### 병합 + DB 저장

| 항목 | 값 |
|------|-----|
| 총 수집 약물 | 3,076건 |
| DB 저장 (score>=10) | 899건 |
| 필터링 (score<10) | 2,177건 |
| 변경 이벤트 | 74건 |
| 스트림 브리핑 | 8건 생성 (GPT-4o-mini) |

### DB 현황 비교

| 지표 | Before (02-13) | After (02-19) | 변화 |
|------|---------------|---------------|------|
| 총 약물 | 818 | **1,215** | +49% |
| 총 이벤트 | 975 | **1,296** | +33% |
| FDA 이벤트 | 335 | **538** | +61% |
| EMA 이벤트 | 569 | **686** | +21% |
| MFDS 이벤트 | 71 | **72** | +1 |
| raw_data non-NULL | 0 | **890** | NEW |
| FDA 승인날짜 | 133 | **254** | +91% |
| EMA 승인날짜 | 227 | **433** | +91% |
| score >= 40 | ~18 | **50** | +178% |
| score >= 20 | — | **496** | — |

### Top 10 약물 (score 기준)

| 약물 | Score | 영역 | 소스 |
|------|-------|------|------|
| POLATUZUMAB VEDOTIN | 60 | oncology | therapeutic + innovation |
| ZANIDATAMAB-HRII | 60 | oncology | therapeutic + innovation |
| ZOLBETUXIMAB | 55 | oncology | therapeutic + innovation |
| IVOSIDENIB | 55 | oncology | therapeutic + innovation |
| TALQUETAMAB | 55 | oncology | therapeutic + innovation |
| MIRVETUXIMAB SORAVTANSINE | 55 | oncology | therapeutic |
| BREXUCABTAGENE AUTOLEUCEL | 55 | oncology | therapeutic + innovation |
| SETMELANOTIDE | 50 | metabolic | therapeutic + innovation |
| SOTATERCEPT-CSRK | 50 | cardiovascular | therapeutic + innovation + external |

---

## 이슈

- **medRxiv 연결 오류**: `peer closed connection without sending complete message body` — 400건 이후 chunked read 실패. 재시도 로직 또는 배치 크기 조정 필요.
- **openFDA Breakthrough 데이터 부재**: 구조적 한계. FDA 공식 Breakthrough 목록 스크래핑 또는 별도 DB 시딩 검토 가능.

---

## 결과물

- `output/pipeline_20260219.log` — 파이프라인 전체 로그 (1,422줄)
- `data/regscan.db` — 1,215 약물, 1,296 이벤트

---

### 7. 기사 품질 벤치마크 분석 + CT.gov resultsSection 구현

**배경**: 약디(yactopia) 뉴스레터 Tradipitant 기사를 벤치마크로 분석. RegScan 기사와의 품질 격차 식별.

**벤치마크 파일**: `data/scraping/benchmarks/yactopia_tradipitant.json`

**핵심 격차 분석**:

| 데이터 항목 | 약디(벤치마크) | RegScan(현재) | 개선 가능 |
|------------|-------------|-------------|----------|
| 임상 1차 종점 (ORR, PFS 등) | 상세 수치 + p-value | 없음 | **CT.gov resultsSection** |
| 안전성 데이터 (TEAE, SAE) | 그룹별 수치 | 없음 | **CT.gov resultsSection** |
| 작용기전 (MOA) | 비전문가 이해 수준 | 없음 | EMA EPAR에서 부분 추출 가능 |
| 개발 과정/역사 | 상세 타임라인 | 없음 | 구조적 한계 |
| 시장 맥락/경쟁약 분석 | 전문가 수준 | 경쟁약 목록만 | 부분 개선 가능 |

**CT.gov resultsSection 구현**:

| 파일 | 변경 |
|------|------|
| `regscan/parse/clinicaltrials_parser.py` | `parse_results_section()`, `_parse_outcome_measure()`, `_parse_adverse_events()` 추가 |
| `regscan/ingest/clinicaltrials.py` | `search_by_intervention()` 메서드 추가, `search_studies()`에 `has_results` 파라미터 추가 |
| `regscan/stream/external.py` | 시그널/약물에 `clinical_results` 전달 |
| `regscan/stream/orchestrator.py` | `merge_results()`에서 `clinical_results` 병합, `build_global_statuses()`에서 status에 주입 |
| `regscan/scan/domestic.py` | `DomesticImpact`에 `clinical_results`, `clinical_results_nct_id` 필드 추가 |
| `regscan/report/llm_generator.py` | `_prepare_drug_data()`에서 임상 결과를 LLM 프롬프트에 주입 |

**검증 결과** (실제 API 테스트):

| 약물 | NCT ID | Primary Endpoint | 결과 |
|------|--------|-----------------|------|
| zolbetuximab | NCT03504397 | PFS | 11.04 vs 8.94개월, HR=0.734, p=0.0024 |
| polatuzumab vedotin | NCT04236141 | Complete Response | 파싱 성공 |
| sotatercept | NCT04576988 | 6MWD Change | 파싱 성공 |
| nivolumab | NCT01721746 | ORR | 27.2% vs 9.8%, HR=0.86 |

---

### 8. 배포 전 점검 + 문서화

**아키텍처 문서**: `docs/architecture.md`
- v3 3-Stream 파이프라인 전체 흐름, 13개 DB 테이블, 12개 API 클라이언트, 스코어링 체계, FastAPI 라우트, 핵심 데이터 클래스 정리

**배포 준비 체크리스트**: `docs/deployment-checklist.md`
- 10개 항목 점검 결과:
  - CRITICAL: `.dockerignore` 없음 (API 키가 Docker 이미지에 포함됨), API 키 로테이션 필요
  - HIGH: CI/CD 파이프라인 없음, 의존성 미고정
  - NEEDS WORK: 17건 silent exception, 구조화된 로깅 없음
  - OK: DB(SQLite+PostgreSQL), Rate Limiting, 테스트(12파일)

**UI/UX 기사 포매팅 개선 계획**: `docs/uiux-improvement-plan.md`
- Gemini 컨설턴트 피드백 3건 분석 + 구현 계획:
  1. 하이퍼링크/출처 (FDA/EMA/CT.gov URL 동적 연결)
  2. 대소문자 일관성 (ALL CAPS vs lowercase → Title Case)
  3. 한국어 현지화 (MFDS→식약처, HIRA→심평원, `<abbr>` 툴팁)
- 권장 순서: 대소문자(2) → 한국어(3) → 하이퍼링크(1)

---

## 다음 할 일

- [ ] **UI/UX 개선 구현**: 대소문자 일관성 → 한국어 현지화 → 하이퍼링크/출처
- [ ] **배포 CRITICAL 조치**: `.dockerignore` 생성, API 키 로테이션, CI/CD 구축
- [ ] medRxiv 연결 안정화 (chunked read timeout/retry)
- [ ] Gemini 결제 설정 후 3-model 재비교
- [x] ~~ClinicalTrials.gov 상세 결과 수집 (resultsSection — ORR, PFS 등)~~ — 완료
- [x] ~~파이프라인 재실행 후 resultsSection 포함 기사 재발행~~ — 완료 (8/20건 CT.gov 결과 주입)
- [ ] EMA EPAR에서 MOA 텍스트 추출
- [ ] 약가 시뮬레이션 (동일 area 기존 급여 약물 가격 통계 기반)
- [ ] `publish_articles.py`의 CT.gov 결과 주입률 개선 (현재 8/20 = 40%)
