# 작업일지 - 2026-03-17

## 작업 내역

### [RegScan] E2E 파이프라인 검증 + v3 KHIDI/KDCA 통합

3/12 Day 2 #5 "통합 E2E 검증" 완료를 위한 파이프라인 실행 및 버그 수정.

#### 1. E2E 파이프라인 실행
- oncology 영역으로 v3 스트림 파이프라인 전체 실행 (603초)
- 결과: 2814개 약물 수집, 631개 DB 적재, V4.1 브리핑 4건 생성 (3 stream + 1 unified)
- OpenAI gpt-5.2 API로 V4.1 Executive Tone 브리핑 정상 생성 확인
- **결과물**: `output/stream_results/stream_2026-03-17_*.json`

#### 2. v3 파이프라인 KHIDI/KDCA 통합 (버그 수정)
- **발견**: KHIDI/KDCA 수집·주입 코드가 레거시 `run_pipeline`에만 존재, v3 `run_stream_pipeline`에는 빠져 있었음
- Step 4.7 추가: KHIDI/KDCA 수집 → NewsArticle 변환 → qualified impacts에 뉴스 주입
- dict/object 양쪽 호환 `_get()` helper 추가 (KHIDI 데이터가 dict 형태)
- KHIDI 30일 수집: 4건 확인 (oncology 키워드 미매칭으로 주입 0건 — 정상 동작)
- KDCA: playwright 미설치로 수집 불가 (로컬 환경 제약)
- **결과물**: `regscan/batch/pipeline.py`

#### 3. GEMINI_MODEL 업그레이드
- `gemini-2.0-flash` → `gemini-2.5-flash` (3/16 비교 테스트 결과 반영)
- **결과물**: `regscan/config/settings.py`

#### 4. compare_v41_models.py 버그 수정 (3/16 잔여)
- `DomesticImpact.quadrant` property setter 에러 수정 (computed property 직접 할당 제거)
- `google-genai` 패키지 설치로 Gemini API 실제 호출 활성화
- **결과물**: `scripts/compare_v41_models.py`

## 커밋

| 커밋 | 내용 |
|------|------|
| 6edc8a3 | feat(pipeline): v3 파이프라인에 KHIDI/KDCA 공공보도자료 수집·주입 통합 |
| d062aa1 | feat(briefing): V4.1 모델 비교 — GPT-5.2 vs Gemini 2.5 Flash vs Gemini 3 Flash |

## 이슈
- KDCA 크롤러: playwright 미설치 (로컬 환경). Cloud Run에서는 정상 작동 예상
- KHIDI 보도자료: 최근 30일 4건이 oncology 키워드와 매칭되지 않음 → 보도자료 양이 적을 때는 주입 0건이 정상

## 다음 할 일
- playwright 설치 후 KDCA 크롤러 로컬 검증
- 실제 oncology 관련 KHIDI/KDCA 보도자료가 있을 때 인용 확인 (수동 테스트 가능)
- 3/12 계획 전체 완료 선언 (Day 1 + Day 2 모두 완료)
