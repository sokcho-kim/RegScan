# 작업일지 - 2026-03-03

## 작업 내역

### 크롤러 근본 수정 — FDA/MFDS 데이터 선택 로직 5건

FDA·MFDS 크롤러의 데이터 선택/파싱 로직에서 발견된 근본적 문제 5건을 일괄 수정.

#### 수정 항목

| # | 문제 | 수정 내용 | 파일 |
|---|------|-----------|------|
| 1 | FDA 교차검색 시 USAN 접미사(-hrii, -piiq 등)가 검색 노이즈 유발 | USAN 접미사 제거 + ORIG+AP 우선순위 일관성 확보 | `regscan/stream/therapeutic.py` |
| 2 | MFDS first-match로 부정확한 제품 선택 | `_select_best_mfds()` 패턴 도입 (유효제품/단독성분/이른허가일 우선) | `regscan/map/global_status.py` |
| 3 | FDA 날짜에 미래/1985년 이전/파싱불가 값 혼입 | 날짜 검증 로직 추가 — 범위 밖 데이터 스킵 | `regscan/parse/fda_parser.py` |
| 4 | DB INN 대소문자·공백 불일치로 중복 발생 | `lower().strip()` 정규화 + 중복 정리 헬퍼 추가 | `regscan/db/loader.py` |
| 5 | breakthrough mock 불일치, e2e_openai 스킵 조건 누락 | 테스트 mock 수정 + 스킵 조건 추가 | `tests/test_stream_innovation.py`, `tests/test_e2e_openai.py` |

#### 변경 규모

- 6개 파일, +2,876 / -2,749 lines (CRLF→LF 정규화 포함)

### Git 환경 정비

| 커밋 | 내용 |
|------|------|
| `.gitattributes` 신규 생성 | `* text=auto eol=lf` — CRLF→LF 자동 정규화, 바이너리(png/jpg) 제외 |
| `.gitignore` 수정 | `._*` 패턴 추가 — macOS resource fork 파일(exFAT 드라이브) 제외 |

## 커밋

| 커밋 | 내용 |
|------|------|
| `583281f` | fix: 크롤러 근본 수정 — FDA/MFDS 데이터 선택 로직 5건 |
| `a78335c` | chore: .gitattributes 추가 — CRLF→LF 자동 정규화 |
| `f2b5826` | chore: .gitignore에 macOS resource fork(._*) 제외 추가 |

## 다음 할 일

- [ ] 한국 뉴스 소스 결정 및 추가 (Google News RSS / Gemini Search grounding)
- [ ] 뉴스 반영 기사 품질 검증 (비교 뷰어로 before/after)
- [ ] C-4 KHIDI 시장 맥락 주입
- [ ] Phase D 프롬프트 V4.1 (Executive Tone)
