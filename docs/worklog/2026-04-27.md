# 작업일지 - 2026-04-27

## 작업 내역

### 1. 기사 파이프라인 LLM 모델 전환
- Gemini 2.5 Pro 1순위 전환 시도 (긴 출력 + 지시준수 목적)
- 쿼타 초과로 롤백, max_completion_tokens=4096 설정
- 커밋: `c55e07e`, `8ff9601`

### 2. 기사 반복 생성 (v1~v12, 12개 파일)
- v1~v8: 기존 depth 기반 프롬프트로 반복 개선
- v9: A/B/C/D 등급 전환 + 정보 축소 금지 + 제목 기준 적용 → 3건
- v10: 국내 크로스레퍼런스 추가 → 팩트체커 과잉 폐기로 **1건만 생존**
- v11: v10과 동일 코드 재실행 → 3건 (LLM 출력 분산 확인)
- v12: 보강 루프 도입 (즉사 제거) → 3건 전부 생존

### 3. 편집 기준 문서화 + 프롬프트 반영
- `docs/research/article-editorial-rules.md`: A/B/C/D 등급, 정보 축소 금지, 제목 기준, 전망 구체화
- 편집장: depth→grade, structural_info, read_reason 필드 추가
- 기자: 등급별 포맷 분기, 전망 구체화 규칙
- 품질 검증: 등급별 기준, structural_info 누락 체크, 추상 전망 탐지
- 국내 관점 확장: "허가/급여 여부"만이 아니라 글로벌 전략 시그널, 구조적 패턴, 국내 임상 포함

### 4. 글로벌→국내 크로스레퍼런스 엔리칭
- `enrichment.py`에 `_enrich_global_with_domestic()` 추가
- NICE/KHIDI 시그널 → 약물 INN 감지 → MFDS 허가 + HIRA 급여 + 국내 임상 자동 조회
- INN→한글 품목명 매핑 (20+약물), ClinicalTrials.gov 한국 사이트 검색

### 5. 보강 루프 설계 (V10 팩트체커 학살 대응)
- 기존: 근거 부족 → 즉시 폐기
- 변경: 근거 부족 → 피드백 → 재작성 (최대 3회) → 그래도 부족하면 등급 다운그레이드
- **절대 폐기 안 함** — A→B→C로 내려서라도 살림

### 6. 자동 버전 넘버링
- `run.py`: 기존 파일 중 최고 버전 +1로 자동 저장
- 절대 덮어쓰기 방지 (v1 날린 사고 재발 방지)

### 7. V5~V12 품질 리뷰 + 기준 문서화
- `docs/research/article-version-review-0427.md`: 버전별 변천사 + 편차 원인 분석
- `docs/research/article-quality-criteria-0427.md`: 좋은/나쁜 기사 기준 + 등급표

### 8. HIRA 약제 기준정보 수집 계획
- `docs/plan/hira-drug-collection-plan.md`: 9개 게시판 통합 수집 설계
- P0: 약제급여평가위원회 + 암질환 공고
- scrape-hub에서 P0 2개 수집 테스트 완료 (`2e50636`)

## 핵심 발견

### 기사 품질 등급표 (최종)
| 파일 | 기사 | 등급 |
|---|---|---:|
| v12 | EC 레미브루티닙 승인 | **B+** |
| v9 | EC 경구 CSU + HTA 변수 | **B** |
| v10 | NICE 비제출 종료 4건 | **B-** |
| v12 | NICE 비제출 종료 5건 | **B-** |
| v11 | 동물병원 마약류 | **D** |

### 편차 원인 3가지
1. **LLM 출력 분산**: 같은 입력인데 V9 마약류(구체적) vs V11 마약류(추상적) — temperature 문제
2. **크로스 소재 연결 비결정적**: V9에서 NICE+CSU 연결은 우연, 재현 안 됨
3. **엔리칭 데이터 미활용**: 국내 크로스레퍼런스 추가했는데 기사에 반영 안 됨 — 디버깅 필요

## 생성된 문서
- `docs/research/article-editorial-rules.md` — 편집 기준 (등급, 제목, 전망, 정보 축소 금지)
- `docs/research/article-version-review-0427.md` — V5~V12 변천사 + 편차 원인
- `docs/research/article-quality-criteria-0427.md` — 좋은/나쁜 기사 기준 + 등급표
- `docs/plan/hira-drug-collection-plan.md` — HIRA 9개 게시판 수집 계획

## 다음 할 일 (집에서 바로 착수)

### P0: 기자 프롬프트에 좋은/나쁜 예시 직접 삽입
- **좋은 예시**: v12 2번 레미브루티닙 (사건→배경→차별점→근거→전망)
- **나쁜 예시**: v11 3번 동물병원 마약류 ("이렇게 쓰면 불합격")
- **나쁜 징후 5개를 품질 검증에 추가**: 추상어 빈도, "기조를 이어간다" 패턴, 발표 사실 반복
- 파일: `regscan/article/pipeline.py` REPORTER_SYSTEM + _evaluate_quality()

### P0: 엔리칭 디버깅
- NICE_TA 시그널에 국내 크로스레퍼런스가 실제로 붙는지 확인
- `_extract_inns_from_text`가 NICE 시그널에서 약물명 매칭하는지 로그 추가
- `format_for_prompt`에서 fda_context가 기자에게 전달되는지 확인
- 파일: `regscan/article/enrichment.py`, `regscan/stream/intelligence_signals.py`

### P1: 크로스 소재 연결 강제
- 편집장 프롬프트에 "같은 약물이 2개 소스에 나오면 반드시 1기사로 연결" 규칙
- 시그널 전처리에서 약물명 기준 클러스터링 후 편집장에게 전달
- 파일: `regscan/article/pipeline.py` EDITOR_CHIEF_SYSTEM

### P1: 신문형 HTML 레이아웃
- 톱기사(A) + 서브기사(B) + 사이드바(카드+목록) 구조
- `output/daily_scan/daily_briefing_2026-02-05_newspaper.html` 참고
- 파일: 새 렌더러 필요 (`regscan/article/renderer.py` 또는 유사)

### P2: 주간 발행 아키텍처
- `--collect-only` / `--publish` 모드
- 매일 수집 → 주 1회 기사 생성

### P2: scrape-hub HIRA 수집기 개선
- 평가위 content 빈값 → 첨부파일 파싱 필요
- 암공고 content 짧음 → 셀렉터 개선
- P1 게시판(약제급여목록표, 공고예고) 추가

### [scrape-hub] HIRA 약제 기준정보 수집기 구현 — `2e50636`

P0 게시판 2개 수집 테스트 완료:
- 약제급여평가위원회: 10건 (INN/제품명/업소명/결과 구조화 파싱, 첨부 40개)
- 암질환 공고: 4건 (공고번호 추출, 첨부 16개)
- 이슈: 평가위 content 빈값 (본문이 첨부파일에 있음), 암공고 content 짧음 → 셀렉터 개선 필요
